<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.A00230757">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CambrianCoursesAppHarpreet">
        <activity android:name=".AddStudentActivity"></activity>
        <activity android:name=".AddProfessorActivity" />
        <activity android:name=".AddCoursesActivity" />
        <activity android:name=".AddDepartmentActivity" />
        <activity android:name=".MainInterface" /> <!-- <activity -->
        <!-- android:name=".SplashScreen" -->
        <!-- android:label="@string/title_activity_splash_screen" -->
        <!-- android:theme="@style/Theme.CambrianCoursesAppHarpreet.NoActionBar"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CambrianCoursesAppHarpreet.NoActionBar" />
    </application>

</manifest>